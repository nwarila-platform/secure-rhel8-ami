name: Release Please

# Optional release entrypoint. Push-triggered release-please is opt-in through
# the RELEASE_PLEASE_ON_PUSH repository variable because GitHub requires an
# explicit repository setting before Actions can create release PRs.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref || github.run_id }}
  cancel-in-progress: false

jobs:
  release-please:
    name: Release Please
    if: ${{ github.event_name == 'workflow_dispatch' || vars.RELEASE_PLEASE_ON_PUSH == 'true' }}
    permissions:
      contents: write
      pull-requests: write
      issues: write
      actions: write
    uses: nwarila-platform/.github/.github/workflows/reusable-release-please.yaml@1923b2d024800b0bd11eb72de9af7c5788ffade1 # nwarila-platform/.github main @ 2026-07-30
